{
  "name": "timer",
  "scripts": {
    "dev": "parcel serve src/index.html",
    "build": "npx rimraf ./dist && parcel build src/index.html",
    "start": "npm run build && npx serve ./dist"
  },
  "devDependencies": {
    "elm": "~0.19.1-3",
    "elm-hot": "^1.1.4",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.4",
    "rimraf": "~3.0.2",
    "serve": "~11.3.0"
  }
}
